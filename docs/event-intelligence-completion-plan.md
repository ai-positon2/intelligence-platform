# Remaining Events & Conference work: four parts

The user authorized completing the remaining engineering and publishing it to GitHub before their final output testing. Keep all changes within this agent and its tests/documentation. No reviewer selection or interim user approval blocks this work. User approval authorizes implementation; it does not constitute factual benchmark labels.

1. **Discovery coverage and report clarity.** Diagnose dropped candidates and incomplete categories; distinguish failed verification from an unattempted search; stop budget-exhausting prompt behavior; make provisional output unmistakable. Validate discovery and report behavior with regressions.
2. **Facts and access.** Strengthen edition-specific evidence and source matching, handle dynamic content and ticket restrictions, and preserve uncertainty. Validate against dated organizer challenge cases, including the observed parent/summit mismatch.
3. **Production reliability and measurement.** Run the updated Position2 benchmark, capture the report and usage/latency, exercise cancellation/recovery safely, and correct demonstrated failures. Keep missing invoice reconciliation explicit rather than inventing costs.
4. **Final integration and release.** Re-run relevant regression and PostgreSQL checks, verify production web/worker versions and health, finish the audit/release record and push all scoped work. Provide one final test path for the user and an honest account of measured limitations.

Business acceptance by the user occurs after these four parts. Independent reviewer labels remain unmeasured unless actual reviews are supplied; they are not prerequisites for completing the authorized engineering work.

## Part 1 implementation

Candidate discovery followed by unfinished verification is now partial research, including when zero events survive. Reports say failed searches returned no usable result rather than asserting they never ran. Incomplete output leads with a provisional headline, and an empty partial run does not claim an empty market. Discovery and confirmation prompts stop when sufficient evidence exists instead of encouraging budget exhaustion. Regression cases preserve true-empty versus unfinished distinctions. Coverage improvement and provider savings must be measured in Part 3; these changes do not establish a recall increase by themselves.

## Part 2 implementation

Source admission now normalizes Unicode/punctuation and “&”/“and”, strips matching year suffixes and explicit parenthesized acronyms, and preserves regional qualifiers and conflicting years. Event names must precede the supported date range locally; another intervening event sentence or a name appearing only in a link destination is insufficient. Common shared-year cross-month ranges are supported. Fragment duplicates no longer consume the second source slot.

Access checks retain individual observations. Explicit hotel/VIP inventory restrictions do not establish whole-event closure when the same page explicitly says general admission or event registration is open. Otherwise access remains unresolved. Application requirements and approval conditions are not treated as confirmed client eligibility. A later event-wide cancellation still blocks admission. Dynamic fallback reads retain hashes and read-mode metadata, without being promoted to rendered-page evidence.

These are deterministic literal-support checks, not semantic proof or browser rendering. Unsupported layouts, formats and ambiguous access remain unverified. Part 3 must measure the resulting coverage and factual accuracy; the bounded organizer challenge check is not the complete benchmark.

## Part 3 measurement support

Full research can be downloaded as JSON through the same account-scoped run-detail response. The file includes source and execution ledgers, partial-report caveats and unverified observations. Downloads are attachments with private/no-store caching. Running or failed reports remain exportable for diagnostics and must not be treated as completed benchmarks.

The Events ledger estimates cost using recorded model IDs and usage at dated standard direct-API list rates. Sonnet 5 uses $2/$10 per million input/output tokens as checked September 10; Sonnet 4.6/4.5 uses $3/$15. Reported searches and cache hits are included. Missing usage, unknown models or cache writes without a recorded duration prevent a complete estimate. Known-call subtotal is separate from the full estimate. Neither is an invoice reconciliation. The original fixed-rate estimate remains labelled as legacy in the exported report.

Run 8 is the fresh production benchmark for Parts 1 and 2. Its results and the cancellation checks are documented in the workspace audit record after completion. Native process-kill/replay and transactional cancellation checks run against disposable PostgreSQL, not by disrupting production services.

### Part 3 observed results and follow-up

The fresh production run completed on its first attempt in approximately 16m 8s, with 30/30 responses recorded. It found five candidates and ranked one; five of six category quotas remained unmet. This establishes measured execution, not adequate discovery recall. Aggregate recorded usage yields an estimated $9.8695 at the dated list rates, without invoice reconciliation.

Live review found hidden reasons for unscored candidates and a Working subtitle after cancellation. Reports now expose stored scoring/admission notes, preserve future unresolved confirmation names/reasons separately from rejections, and label terminal recommendation/workroom states accurately. No old missing evidence is synthesized. The full JSON attachment route passed account-isolation tests, but Chrome blocked the live attachment navigation; browser download completion remains unverified.

A separate live cancellation test was cancelled with four calls in flight. At 135 seconds after cancellation all four responses were recorded, but the job remained cancelled, with zero stages and zero saved candidates. No stale report overwrite occurred. Recovery from process death remains covered by disposable PostgreSQL subprocess tests; production processes were not killed.

## Part 4 validation and handoff

The final local Events regression passed 1,305 tests; 81 environment-dependent tests were skipped locally and PostgreSQL checks run separately in CI. Final release requires passing that CI and verifying the existing production web/worker deployments. Changes remain confined to Events code, its tests, workflow and documentation.

User test path after release: open Events & Conference Intelligence on intelligence.position2.com, choose the saved Position2 client, enter the offer, and run Score the calendar. Review the completion caveat first, then event dates/source links, score explanations, Not scored reasons and category gaps. The completed benchmark is run8; run9 is intentionally cancelled. Existing research is retained and is not silently rescored by these releases.

Engineering validation must not be mistaken for business acceptance. Complete recall, independently reviewed relevance/critical facts, actual attendance/access, and invoice reconciliation remain unmeasured. Dynamic source fallback and unusual page layouts can still withhold valid events. The measured run produced a provisional single-event recommendation, so this release is suitable for supervised research review; it has not established reliable comprehensive event selection without human review.
